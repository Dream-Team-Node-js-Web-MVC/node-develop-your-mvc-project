const { User } = require("./userModel");
const { Worker } = require("./workerModel");
const { Product } = require("./productModel");

module.exports = {
  User: User,
  Worker: Worker,
  Product: Product,
};
